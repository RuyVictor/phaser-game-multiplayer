import { containerHTML } from './html'
export default class WhoKilledWho extends Phaser.GameObjects.DOMElement {

    constructor (scene: Phaser.Scene, count: number, x: number, y: number) {
        super(scene, );
        this.createFromHTML(containerHTML)
        const a = this
    }
}